[package]
name = "capcap"
version = "0.1.0"
edition = "2021"
description = "Zero-cloud, local-first video captioning, translation & container muxing"
license = "MIT"
repository = "https://github.com/matheusbuniotto/caption-captain"

[dependencies]
clap = { version = "4", features = ["derive"] }
